import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sebilling.fibercalc',
  appName: 'Fiber Optic Calculator',
  webDir: 'out'
};

export default config;
